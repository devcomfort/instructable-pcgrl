# 애플리케이션 제목 설정 (HTML 태그 사용 가능, 예: RLBrush <sup>+</sup>)
# Application title configuration (HTML tags supported, e.g., RLBrush <sup>+</sup>)
VITE_TITLE="Your App Name"

# API 서버 기본 URL 설정
# Base URL for API server
VITE_API_BASE="https://api.example.com"

# 후보군 애니메이션 모드 활성화 여부 (true/false)
# Enable animation in map candidates list
VITE_CANDIDATE_ANIMATION_MODE="true"

# 선택 시 애니메이션 모드 활성화 여부 (true/false)  
# Enable animation when selecting a map candidate
VITE_SELECTION_ANIMATION_MODE="true"

# 후보군 애니메이션 간격 (밀리초)
# Animation interval for map candidates in milliseconds
VITE_CANDIDATE_ANIMATION_INTERVAL="600"

# 선택 애니메이션 간격 (밀리초)
# Animation interval for selection animation in milliseconds
VITE_SELECTION_ANIMATION_INTERVAL="400"

# 데이터가 없을 경우 임시 에이전트 위치 표시 여부 (true/false)
# Show a temporary agent position (e.g., center of the map) if actual agent data is missing (true/false)
VITE_SHOW_AGENT_POSITION=false 

# --- 타일 생성 확률 (초기 랜덤 맵 생성 시) ---
# 각 타일(EMPTY, WALL, BAT)이 생성될 기본 확률을 0.0에서 1.0 사이의 실수로 지정합니다.
# 실제 적용 시 이 값들의 합계를 기준으로 정규화되어 사용됩니다 (즉, 합이 1이 아니어도 됨).
# 예를 들어, EMPTY=0.7, WALL=0.2, BAT=0.1로 설정하면, 전체 확률 중 EMPTY가 70%를 차지하게 됩니다.
# 모든 값이 0으로 설정되거나 환경 변수가 없는 경우, 기본값 (EMPTY: 0.7, WALL: 0.2, BAT: 0.1)이 사용됩니다.

# EMPTY 타일 생성 확률
VITE_PROBABILITY_FOR_EMPTY="0.7"

# WALL 타일 생성 확률
VITE_PROBABILITY_FOR_WALL="0.2"

# BAT 타일 생성 확률
VITE_PROBABILITY_FOR_BAT="0.1"